class Transaction {
  String id;
  int iconId;
  String title;
  String description;
  num amount;
  String date;
  Transaction(this.id, this.iconId, this.title, this.description, this.amount,
      this.date);
}
